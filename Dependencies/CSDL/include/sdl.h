#define SDL_MAIN_HANDLED 1
#define SDL_MAIN_USE_CALLBACKS 1

#include <SDL3/SDL.h>
#include <SDL3/SDL_main.h>


/*
 static inline bool SDLCALL SDL_SetError(const char *fmt, va_list ap) {
 return fcntl(fd, cmd, value);
 }
 extern SDL_DECLSPEC bool SDLCALL SDL_SetError(SDL_PRINTF_FORMAT_STRING const char *fmt, ...);
 */
